# DCM PROCESSOR
A dicom processing library setup with docker containers.

## DEPENDENCIES
1. Docker & Docker Compose

## Getting up and running
Do not use this repo directly rather install the `dcm-processor` python command line tool with `pip install dcm-processor`
Read the documentation at...